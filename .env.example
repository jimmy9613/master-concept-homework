# 複製這個檔案並改名為 .env，填入你的 API 金鑰。
# .env 只放在伺服器上，千萬不要上傳到 GitHub 或放進前端程式碼。

# Gemini：https://aistudio.google.com/apikey
# 在 AI Studio 申請、且沒有啟用計費的金鑰使用免費方案，不會被收費（超過額度只會暫時無法使用）。
GEMINI_API_KEY=

# 以下為選填
# 模型（預設 gemini-3.5-flash-lite，免費方案每天約 500 次；gemini-3.8-flash 品質較好但每天只有約 20 次）
# GEMINI_MODEL=gemini-3.5-flash-lite
# PORT=8000
